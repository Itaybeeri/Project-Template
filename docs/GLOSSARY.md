# Glossary

Canonical terms for this project. **Use these words exactly** — distinct concepts get
distinct words; no term means two things; no two terms mean one thing. Update this file
in the same change that introduces or renames a term (FEATURE-RULES design rule 6).

## Process terms (from the template — keep or adjust)

- **Feature** — a unit of work that flows through the five-phase lifecycle; lives in
  `docs/features/<NNNN-name>/`.
- **ADR** — Architecture Decision Record; one architecturally significant decision + why
  (`docs/adr/`, immutable, append-only).
- **Architect review** — the AI gate after Brainstorm (design soundness).
- **Plan review** — the AI gate after Plan (spec correctness/completeness).
- **Merge gate** — the completion bar: every acceptance criterion COVERED + green CI.

## Domain terms

<FILL: your domain vocabulary — one bullet per term.>
- **<term>** — <definition>.
